test:
    subq $8, %rsp
    movq %r11, (%rsp)
    cbl $5, %rdi, .L2
    movq $0, %edx
    jmp .L1
.loop:
    addq %edx, %esi
    addq $1, %edx
.L1:
    leaq 1(%rdi), %rcx
    cbg %rcx, %rax, .loop
    cbl $14, %esi, .L2
    subq $8, %rsp
    movq %rcx, %rdi
    call test, %r11
    movq $15, %eax
    addq $8, %rsp
    movq (%rsp), %r11
    addq $8, %rsp
    ret %r11
.L2:
    movq $15, %eax
    movq (%rsp), %r11
    addq $8, %rsp
    ret %r11
