test:
    subq $8, %rsp
    movq %r11, (%rsp)
    cbge $1, %rdi, .L3
    subq $16, %rsp
    movq %rbp, 8(%rsp)
    movq %rbx, (%rsp)
    subq $8, %rsp
    movq %rdi, %rbx
    leaq -1(%rdi), %rdi
    call test, %r11
    stop
.L3:
    movq $1, %eax
    movq (%rsp), %r11
    addq $8, %rsp
    ret %r11
    stop
.L2:
    addq $8, %rsp
    movq (%rsp), %rbx
    movq 8(%rsp), %rbp
    movq 16(%rsp), %r11
    addq $24, %rsp
    ret %r11
    stop